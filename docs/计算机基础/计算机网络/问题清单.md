## JavaGuide
## 计算机网络基础
+ OSI 七层模型是什么？每一层的作用是什么？
+ TCP/IP 四层模型是什么？每一层的作用是什么？
+ 为什么网络要分层？
+ 应用层有哪些常见的协议？
+ 传输层有哪些常见的协议？
+ 网络层有哪些常见的协议？

## 应用层
+ 从输入 URL 到页面展示到底发生了什么？
+ HTTP 状态码有哪些？
+ HTTP Header 中常见的字段有哪些？
+ HTTP 和 HTTPS 有什么区别？（重要）
    - 端口号
    - URL前缀
    - 安全性和资源消耗
    - SEO搜索引擎优化
+ HTTP/1.0 和 HTTP/1.1 有什么区别？
    - 支不支持长连接
    - 状态响应码
    - 缓存机制
    - 带宽/断点续传/请求资源的一部分
    - Host请求头/同一IP地址托管多个域名【虚拟主机技术 -- 多态主机可以共用同一IP】
+ HTTP/1.1 和 HTTP/2.0 有什么区别？
    - 多路复用
    - 二进制帧
    - 头部压缩
    - 服务器推送
+ HTTP/2.0 和 HTTP/3.0 有什么区别？
    - 传输协议（基于QUIC协议）
    - 连接建立仅需0-RTT或1-RTT
    - 头部压缩
    - 队头阻塞
    - 连接迁移
    - 错误恢复
    - 安全性
+ HTTP 是不保存状态的协议, 如何保存用户状态?
+ Cookie 被禁用怎么办?
+ URI 和 URL 的区别是什么?
+ GET 和 POST 的区别
    - 语义
    - 幂等
    - 请求参数位置
    - 缓存
    - 安全性
+ 什么是 WebSocket?
+ WebSocket 和 HTTP 有什么区别？
+ WebSocket 的工作过程是什么样的？
+ SSE 与 WebSocket 有什么区别？
+ SSE 与 WebSocket 该如何选择？
+ PING 命令的作用是什么？
+ PING 命令的工作原理是什么？
+ DNS 的作用是什么？
+ DNS是在什么协议之上运行的，
+ DNS 服务器有哪些？根服务器有多少个？
+ DNS 解析的过程是什么样的？
+ DNS 劫持了解吗？如何应对？

## 传输层
### TCP和UDP
+ TCP 与 UDP 的区别（重要）
    - 是否面向连接
    - 是否是可靠传输
    - 是否有状态
    - 传输效率
    - 传输形式
    - 首部开销
    - 是否提供广播或多播服务
+ 什么时候选择 TCP，什么时候选 UDP?
+ HTTP 基于 TCP 还是 UDP？
+ 使用 TCP 的协议有哪些?使用 UDP 的协议有哪些?

### TCP 三次握手和四次挥手
+ 三次握手，四次挥手的流程是怎么样的？
+ 什么是半连接队列？什么是全连接队列？
+ 为什么要三次握手?
+ 第 2 次握手传回了 ACK，为什么还要传回 SYN？
+ 三次握手过程中可以携带数据吗？
+ 为什么要四次挥手？
+ 为什么不能把服务器发送的 ACK 和 FIN 合并起来，变成三次挥手？
+ 如果第二次挥手时服务器的 ACK 没有送达客户端，会怎样？
+ 为什么第四次挥手客户端需要等待 2*MSL（报文段最长寿命）时间后才进入 CLOSED 状态？

> 防止服务端没有收到最后一次挥手的ACK重发FIN，如果提前关闭，客户端就无法处理。
>

### TCP 传输可靠性保障
+ TCP 如何保证传输的可靠性？
+ TCP 如何通过滑动窗口机制实现流量控制？
+ TCP 的拥塞控制是怎么实现的？
+ 什么是ARQ协议？什么是停止等待ARQ协议？什么是连续ARQ协议？
+ 超时重传如何实现？超时重传时间怎么确定？

## 网络层
+ IP 协议的作用是什么？
+ 什么是 IP 地址？IP 寻址如何工作？
+ 什么是 IP 地址过滤？
+ IPv4 和 IPv6 有什么区别？
+ 如何获取客户端真实 IP？
+ NAT 的作用是什么？
+ 什么是 Mac 地址？
+ ARP 协议解决了什么问题？
+ ARP 协议的工作原理？

## 小林coding
+ 如何唯一确定一个 TCP 连接呢？
+ 一台机器TCP的最大连接数
+ TCP 和 UDP 可以使用同一个端口吗？
+ <font style="color:rgb(44, 62, 80);">多个 TCP 服务进程可以同时绑定同一个端口吗？</font>
+ <font style="color:rgb(44, 62, 80);">重启 TCP 服务进程时，为什么会出现“Address in use”的报错信息？又该怎么避免？</font>
+ <font style="color:rgb(44, 62, 80);">客户端的端口可以重复使用吗？</font>
+ <font style="color:rgb(44, 62, 80);">客户端 TCP 连接 TIME_WAIT 状态过多，会导致端口资源耗尽而无法建立新的连接吗？</font>
+ <font style="color:rgb(44, 62, 80);">如何在 Linux 系统中查看 TCP 状态？</font>
+ <font style="color:rgb(44, 62, 80);">什么是 SYN 攻击？如何避免 SYN 攻击？</font>

<font style="color:rgb(44, 62, 80);">  
</font><font style="color:rgb(44, 62, 80);"> </font>

<font style="color:rgb(44, 62, 80);">  
</font><font style="color:rgb(44, 62, 80);"> </font>

  
 

