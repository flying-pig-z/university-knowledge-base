<meta name="referrer" content="no-referrer"/>

算法题无非就是算法/思路+代码功底【就像工程无非就是设计+实现】，代码复现的功底C和C++和数据结构上机课一路下来早已轻车熟路，无法就是进一步熟悉Java中集合的使用。

能不能想到题目对应的算法/思路成为解题的瓶颈，有了思路就能很快复现。由于本人记忆力不行，这里记下各题的思路以便快速复习。

1. 前 K 个高频元素 -- HashMap统计 + 优先队列排序

> + 为什么不用快速排序，要用堆排序/优先队列？
> + 优先队列使用哪个集合？
> + 小顶堆怎么写？大顶堆怎么写？
>

2. 递归遍历的三要素
3. 层序遍历：使用队列，移除父节点，引入子结点（注意是使用queue size区分每一层）【很多树的题目其实用层序遍历很容易做】
4. 最大深度 最小深度 -- 层序遍历
5. 摆动序列

> + 先跳过不连续计算连续（计算峰值 -- 即最高峰数量+最低谷数量+1）  
【preDiff<0&&curDiff>0 || preDiff>0&&curDiff<0】
> + 考虑上下坡中有平坡特殊情况修改条件成  
【preDiff <= 0 && curDiff > 0) || (preDiff >= 0 && curDiff < 0】
> + 只在摆动变化的时候更新prediff = curDiff
>

> 做题要区分子序列和连续子序列
>

6. 最长连续序列

> 1. 使用HashSet去重并支持O(1)查找
> 2. 对于每个数x，如果x-1不存在，说明x是一个序列的起点
> 3. 从这个起点往后查找连续的数，更新最大长度
>

6. Z字形变换

> [https://leetcode.cn/problems/zigzag-conversion/solutions/21610/zzi-xing-bian-huan-by-jyd/](https://leetcode.cn/problems/zigzag-conversion/solutions/21610/zzi-xing-bian-huan-by-jyd/)
>
> 看题解动图，立flag，根据flag的变换引导方向
>

509. 斐波那契数

> 简单的动态规划，直接创建数组然后按照题目模拟就行了
>

70. 爬楼梯

> 动态规划，最后一层数量 = 倒数第一层数量 + 倒数第二层数量，因为一次可以爬一格或者两格
>

1. 两数之和

> 确定一个数，哈希表找另一个数
>





