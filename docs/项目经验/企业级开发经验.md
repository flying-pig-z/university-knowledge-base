<meta name="referrer" content="no-referrer"/>

## 多数据源
一般是在MyBatis的配置文件中指定哪个文件夹下的mapper对应什么数据库。

拆分数据库有助于不同数据库负责不同的事，并且可以防止一个数据库过大等等有点

## 数据隔离组件
一个是一个注解，加上注解之后可以拦截这个方法执行的所有SQL语句(SELECT、UPDATE、DELETE)，自动添加 WHERE tenant_id = 'xxx' 条件，这样保证租户之间数据隔离。

当然还有比较重量级的比如一个租户一张表/一个租户一个数据库，但是用户量大的情况下不太现实。

## 冷热分离
简单说就是根据更新时间/创建时间，把一些旧的数据归档，比如笔记网站的一些笔记版本，企鹅的很早的聊天记录可以从Mysql中删除，转储到可以存储更多数据的HIVE表。

一般定时扫描删除Mysql中的数据转储到HIVE表。

## 数据同步
不同服务之间的资源一般是隔离的，就是一个服务不能调其他服务的数据库。

所以我们就需要从其他服务同步数据到自己的系统。一般是实时流增量同步和离线全量同步结合，然后还会有对账和手动修复兜底。

## 灰度
选取部分用户使用某个接口/功能。一般是随机选取，比如要选取百分之十的用户，可以根据userId取余10。

```plain
if ( userId % 10 == 0 ) {
  // 选取十分之一的用户
  doA();
} else {
  doB();
}
```

## AB测试
还不大懂，对比测试

## 多级缓存
Redis + 本地缓存

一般以为Redis更新通知各个服务本地缓存删除/更新，但是看了一些项目，好像都是通过TTL删除，本地缓存设置一个业务可以接受的TTL。

## 聚合写入
像比如点击量 点赞数这类的统计就可以使用Flink之类的工具聚合写入，减少Mysql的写入量。

