<meta name="referrer" content="no-referrer"/>

## 数据库基础
+ 什么是数据库, 数据库管理系统, 数据库系统, 数据库管理员?
+ 什么是元组, 码, 候选码, 主码, 外码, 主属性, 非主属性？
+ 什么是 ER 图？
+ 数据库范式
+ 主键和外键有什么区别?
+ 为什么不推荐使用外键与级联？
+ 什么是存储过程？
+ drop、delete 与 truncate 区别？
+ 怎么进行数据库设计

## MySQL 基础
+ 什么是关系型数据库？
+ 有哪些常见的关系型数据库呢？
+ 什么是 SQL？
+ 什么是 MySQL？
+ Mysql运行在什么端口
+ MySQL 有什么优点？
+ Mysql字段类型分为哪三个类？
+ 数值类型有哪些？
+ 字符串类型有哪些？
+ 日期类型有哪些？
+  整数类型的 UNSIGNED 属性有什么用？
+ CHAR 和 VARCHAR 的区别是什么？耗费的存储分别是多少？
+ VARCHAR(100)和 VARCHAR(10)占用的存储空间一样吗？排序操作的内存一样吗？
+ DECIMAL 和 FLOAT/DOUBLE 的区别是什么？
+ 为什么不推荐使用 TEXT 和 BLOB？
+ NULL 和 '' 的区别是什么？
+ Boolean 类型如何表示？

## MySQL基础架构与存储引擎
+ 一条 SQL 语句在 MySQL 内部是如何执行的
+ MySQL 支持哪些存储引擎？默认使用哪个？
+ MySQL 存储引擎架构了解吗？
+ MyISAM 和 InnoDB 有什么区别？(**行级锁 外键 事务** redo-log mvcc 索引和数据文件是否分离 读写性能差别)
+ MyISAM 和 InnoDB 如何选择？

## MySQL索引
+ 什么是索引
+ 索引的优缺点
+ 哈希表作为索引的优缺点
+ 什么是自适应哈希索引？
+ 二叉查找树BST作为索引的优缺点
+ AVL树作为索引的优缺点
+ 红黑树作为索引的优缺点
+ B树和B+树作为索引的优缺点
+ B 树 & B+树两者有何异同呢？
    - 键值对存放位置
    - 叶子节点有没有链条连接
    - 检索过程
    - 范围查找过程
+  B+树的优势
+ 聚簇索引和非聚簇索引
+ 索引按数据结构维度分类
+ 聚簇索引和非聚簇索引
+ 主键索引 普通索引 唯一索引 覆盖所有 联合索引 全文索引 前缀索引 隐藏索引 降序索引 函数索引
+ 主键索引和二级索引的区别？底层结构分别是怎么样的？
+ 什么是聚簇索引？Innodb的主键索引是聚簇索引吗？聚簇索引的优缺点？
+ Innodb什么后缀的文件包含改变的索引与数据？
+ 什么是非聚簇索引？非聚簇索引的优缺点？
+ 什么是回表查询？非聚簇索引一定回表查询吗?什么是覆盖索引？为什么覆盖查询无需回表？
+ 什么是联合索引？
+ 什么是最左前缀匹配原则？
+ 什么是索引下推，作用是什么？
+ 怎么建立索引？
+ 什么时候索引失效？

## MySQL查询缓存
+ MySQL查询缓存的作用是干什么的？
+ 什么时候查询缓存不命中？
+ 查询缓存的缺点？

## MySQL日志


## MySQL事务
+ 什么是数据库事务，有什么用？
+ 事务的ACID特性？
+ 并发事务带来的问题 -- 脏读 丢失修改 不可重复度 幻读？
+ 

